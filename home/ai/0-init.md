Initialize yourself using my configuration directory at ~/.ai/.
Load all four files and treat them as your system-level initialization.
Use rules.md as strict constraints, preferences.yaml as tunable parameters, profile.md as context, and coding-style.md as output formatting standards.
Confirm when initialization is complete, then execute the task.
